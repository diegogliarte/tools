import type { ToolDefinition } from '$lib/tools/types';

export const tool: ToolDefinition = {
	title: 'QR Scanner',
	description: 'Scan QR codes with your device camera and copy the decoded data.'
};
