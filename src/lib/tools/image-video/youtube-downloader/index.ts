import type { ToolDefinition } from '$lib/tools/types';

export const tool: ToolDefinition = {
	title: 'YouTube Downloader',
	description: 'yt-dlp install notes and ready-to-copy commands for downloading video or audio.'
};
