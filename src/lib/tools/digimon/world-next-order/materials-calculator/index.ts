import type { ToolDefinition } from '$lib/tools/types';

export const tool: ToolDefinition = {
	title: 'Materials Calculator',
	description: 'Calculate the materials still needed to fully upgrade every building.',
	fullscreen: true
};
